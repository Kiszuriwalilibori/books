import { render, screen } from "../../../testing-library-utils";
import userEvent from "@testing-library/user-event";
import { cleanup, act } from "@testing-library/react";
import { filtrate } from "../../../../../src/js/BooksManager/tableHelpers/filtrate";
import { ColumnHeaders } from "../../../../../src/models/Column";

describe("Given filtrate function", () => {
    describe("when called with given props", () => {
        it("returns correctly filtered data", () => {});
    });
});
